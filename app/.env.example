# Mapbox API tokens
MAPBOX_ACCESS_TOKEN=your_secret_mapbox_token_here             # Server-side only (sk...)
NEXT_PUBLIC_MAPBOX_PUBLIC_TOKEN=your_public_mapbox_token_here # Client-side (pk...)

# Other API keys can be added here (always keep these server-side only)
# AIS_STREAM_API_KEY=your_ais_stream_api_key
# MAXAR_API_KEY=your_maxar_api_key